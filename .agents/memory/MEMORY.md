- [IshuTools import quirks](import-quirks.md) — pdfminer.layout does NOT have LTAnon or LTTable; use only LTTextBox
- [Drop zone click fix](dropzone-fix.md) — Always add explicit click handler on drop zone div; CSS overlay alone unreliable across browsers
- [Hero layout fix](layout-fix.md) — Never use min-height:100vh on hero; hides tools below fold. Use padding only.
- [GSAP opacity danger](gsap-opacity.md) — Never use opacity:0 in gsap.from() for above-fold elements; use y-only animations so content is always visible
- [Tool card navigation](tool-card-nav.md) — Cards are <a> links; add path field to TOOLS for mismatched folder names; all tool folders need empty style.css+script.js to avoid 404s
- [Tool path redirects](tool-redirects.md) — app.py TOOL_REDIRECTS dict handles renamed tool URLs with 301 redirects; summarize-pdf canonical, ai-summarizer 301 → summarize-pdf
- [CSS edit exact match](css-edit-exact.md) — main.css section headers use em-dashes with varying dash counts; always grep exact line before edit() to avoid mismatch
- [Card visibility fix](card-visibility.md) — NEVER use IO-based opacity:0 reveal on tool cards; use CSS animation with stagger delay instead (animation-delay inline style set in buildCardHTML by idx)
- [Typed.js defer loading](typed-js.md) — Typed.js loaded with defer is safe; call initTyped via setTimeout(900ms) inside DOMContentLoaded so defer scripts are guaranteed loaded
- [Marquee strip pattern](marquee-strip.md) — Duplicate marquee-row with aria-hidden="true" for seamless loop; use min-width:100% + animation-delay:-halfDuration for the duplicate
- [Counter IntersectionObserver](counter-io.md) — initCounters uses IO with threshold:.5; counters appear mid-animation in screenshots (correct — they animate on scroll-into-view)
- [redact-pdf accent color](redact-color.md) — redact-pdf originally used #1F2937 (near-black); changed to #64748B/#475569 for visible gradient on dark background
- [TOOLS array completeness](tools-array.md) — JS TOOLS array (main.js) must have entry for EVERY tool; counter data-count in index.html set to 36; 36 tools confirmed via /api/health; ai-summarizer uses apiEndpoint:'/api/summarize-pdf'
- [Scroll reveal opacity danger](scroll-reveal.md) — NEVER use opacity:0 in reveal-up/IO patterns; use CSS @keyframes y-only animation so content is always readable during transitions
- [SEO hidden div penalty](seo-hidden.md) — Replaced position:absolute opacity:0 hidden SEO div with visible seo-tools-dir section; cloaking is penalized by Google; visible structured dirs are canonical
- [Tool .py file scale](tool-py-scale.md) — All 34 tool .py files have 28-42 functions each (953 total, 41k+ lines); append new functions at bottom; always syntax-check with compile() before restart
- [API route naming](api-routes.md) — Route names differ from UI names: page-numbers→add-page-numbers, watermark→add-watermark, pdf-to-jpg→pdf-to-img; always verify in app.py before testing
- [Merge PDF enterprise features](merge-pdf-enterprise.md) — pdf_merge.py has validate_for_merge(), generate_thumbnail_b64(), smart_postprocess(); merge_pdfs() accepts file_names for TOC/separator titles; display_names parsed in app.py and passed through
- [Merge PDF sounds architecture](merge-sounds-arch.md) — All sounds live in sounds/sounds.js as IIFE exporting window.SOUNDS global; 17 sounds total; script.js calls SOUNDS.* at correct moments; sounds.js loaded as regular <script> (not defer) so it's ready when script.js runs; sound toggle button uses .icon-toggle CSS class + .sound-off state
- [Merge presets pattern](merge-presets.md) — MERGE_PRESETS const + applyPreset(key) fires change events on both qOpt chips AND opt checkboxes; querySelector preset-btn forEach wired after definition; preset.active class for visual state
- [Preview modal PDF.js](preview-modal.md) — openPreview/renderPreviewContent/closePreview; pdfDoc from arrayBuffer with password; renders first 10 pages at scale 0.65 into pv-pages-grid; image files use createObjectURL; GSAP y-only enter/exit
- [Undo delete pattern](undo-delete.md) — pushUndo(entry,idx) stores up to 5 items in _deletedStack; undoLastDelete() splices back at captured idx; showUndoBar()/hideUndoBar() with 4.5s auto-hide; Ctrl+Z keyboard shortcut
- [Touch swipe delete](touch-swipe.md) — addTouchSwipe(card,entryId): touchstart/move/end; dx>=140 → auto-delete; dx>=72 → snap-open with tap-to-dismiss; swipe-hint div inside each card; display:none on hover:hover devices
- [Merge PDF smart range parser](merge-smart-range.md) — _parse_range() in pdf_merge.py handles 'odd','even','first N','last N','first','last' plus numeric ranges; 'all'/'' returns all pages; app.py must pad page_ranges list to len(paths) with 'all'
- [Merge PDF output filename](merge-output-filename.md) — JS sends output_filename form field (smartOutputFilename() = optFilename override OR firstFile_merged.pdf); app.py uses secure_filename() + appends .pdf if missing; server download_name set accordingly
- [Merge PDF image support](merge-image-support.md) — pdf_merge.py has convert_image_to_pdf_bytes(), convert_image_to_pdf_file(), batch_images_to_pdf(); app.py pre-converts images before merge using IMAGE_EXTS set; JS sends file_types JSON array; img2pdf used first (lossless), Pillow+ReportLab as fallback
- [Merge PDF simplified cards](merge-card-ux.md) — File cards show only name+size+type by default; expand button (.fc-btn.expand-btn) reveals page range, password, display name fields; images show 'auto-converted' message instead of page range; card CSS uses fc-expand hidden by default, shown when .expanded class added
- [Merge PDF sounds creation](merge-sounds-creation.md) — sounds/sounds.js MUST be created as IIFE in frontend/tools/merge-pdf/sounds/; use Web Audio API synthesis (no mp3/wav files); loaded as regular script tag (not defer) before script.js; storage key 'ishu-sounds-v2'; SOUNDS global with 18+ functions including toggle/isEnabled
- [Merge PDF response headers](merge-response-headers.md) — backend sets X-Total-Pages, X-Source-Count, X-Method-Used, X-Output-Size, X-Linearized + Access-Control-Expose-Headers; frontend reads via resp.headers.get() after fetch; use these instead of client-side estimates for result stats
- [Tool page base href](tool-base-href.md) — Flask serves tool pages at /tools/<name> (no trailing slash); relative paths in HTML resolve from /tools/ not /tools/<name>/; fix: add <base href="/tools/<name>/"/> as first element after charset meta
- [SSE progress pattern](sse-progress.md) — _progress_queues dict in app.py; _push_progress(job_id,pct,title,sub,done) puts to queue; SSE endpoint reads queue with 1.5s timeout, yields ping heartbeats; progress_cb=file_progress_cb passed into merge_pdfs(); simulate fallback in JS with setInterval
